name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Portolan CLI
        # --pre because portolan-cli is currently pre-release (1.0.0aN).
        run: python -m pip install --pre portolan-cli

      - name: Run catalog tests (incl. STAC validation)
        run: |
          python3 tests/test_links.py
          python3 tests/test_git_ext.py
          python3 tests/test_publish.py
          python3 tests/test_scaffolds.py
          python3 tests/test_stac_valid.py
