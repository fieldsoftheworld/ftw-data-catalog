# FTW field-boundary predictions — Ethiopia

Country code: ET. Partitions: 37.

Each polygon carries a `confidence` value (0–100): the modeled PRUE confidence sampled at the field's point-on-surface from the 500 m confidence COG (the `predictions/confidence` collection), rescaled `raw / 0.578178 * 100` and clamped to 100 (cells with no data become null). 0.578178 is treated as 100%, matching the FTW inference app; this centroid sample is the closest single value to the app's `confidence_mean`. Recommended default reliability filter: confidence >= 69 (raw 0.4). Confidence reflects cell-level model reliability, not individual-polygon geometric accuracy. Generated by `add_confidence.py` (+ `process_partition.sh`, `run_rails.sh`, `make_pmtiles.py`): https://github.com/fieldsoftheworld/ftw-portolan/blob/main/scripts/add_confidence.py

## Files
- `ET_24.parquet` (GeoParquet) + `ET_24.pmtiles` (vector tiles)
- `ET_25.parquet` (GeoParquet) + `ET_25.pmtiles` (vector tiles)
- `ET_43.parquet` (GeoParquet) + `ET_43.pmtiles` (vector tiles)
- `ET_46.parquet` (GeoParquet) + `ET_46.pmtiles` (vector tiles)
- `ET_AA.parquet` (GeoParquet) + `ET_AA.pmtiles` (vector tiles)
- `ET_AF.parquet` (GeoParquet) + `ET_AF.pmtiles` (vector tiles)
- `ET_AM.parquet` (GeoParquet) + `ET_AM.pmtiles` (vector tiles)
- `ET_AS.parquet` (GeoParquet) + `ET_AS.pmtiles` (vector tiles)
- `ET_AW.parquet` (GeoParquet) + `ET_AW.pmtiles` (vector tiles)
- `ET_BE.parquet` (GeoParquet) + `ET_BE.pmtiles` (vector tiles)
- `ET_BK.parquet` (GeoParquet) + `ET_BK.pmtiles` (vector tiles)
- `ET_DD.parquet` (GeoParquet) + `ET_DD.pmtiles` (vector tiles)
- `ET_DI.parquet` (GeoParquet) + `ET_DI.pmtiles` (vector tiles)
- `ET_DK.parquet` (GeoParquet) + `ET_DK.pmtiles` (vector tiles)
- `ET_DU.parquet` (GeoParquet) + `ET_DU.pmtiles` (vector tiles)
- `ET_EE.parquet` (GeoParquet) + `ET_EE.pmtiles` (vector tiles)
- `ET_GA.parquet` (GeoParquet) + `ET_GA.pmtiles` (vector tiles)
- `ET_GB.parquet` (GeoParquet) + `ET_GB.pmtiles` (vector tiles)
- `ET_GD.parquet` (GeoParquet) + `ET_GD.pmtiles` (vector tiles)
- `ET_GE.parquet` (GeoParquet) + `ET_GE.pmtiles` (vector tiles)
- `ET_HA.parquet` (GeoParquet) + `ET_HA.pmtiles` (vector tiles)
- `ET_HI.parquet` (GeoParquet) + `ET_HI.pmtiles` (vector tiles)
- `ET_JG.parquet` (GeoParquet) + `ET_JG.pmtiles` (vector tiles)
- `ET_KA.parquet` (GeoParquet) + `ET_KA.pmtiles` (vector tiles)
- `ET_MU.parquet` (GeoParquet) + `ET_MU.pmtiles` (vector tiles)
- `ET_NB.parquet` (GeoParquet) + `ET_NB.pmtiles` (vector tiles)
- `ET_NU.parquet` (GeoParquet) + `ET_NU.pmtiles` (vector tiles)
- `ET_OR.parquet` (GeoParquet) + `ET_OR.pmtiles` (vector tiles)
- `ET_SI.parquet` (GeoParquet) + `ET_SI.pmtiles` (vector tiles)
- `ET_SK.parquet` (GeoParquet) + `ET_SK.pmtiles` (vector tiles)
- `ET_SO.parquet` (GeoParquet) + `ET_SO.pmtiles` (vector tiles)
- `ET_SW.parquet` (GeoParquet) + `ET_SW.pmtiles` (vector tiles)
- `ET_TA.parquet` (GeoParquet) + `ET_TA.pmtiles` (vector tiles)
- `ET_TI.parquet` (GeoParquet) + `ET_TI.pmtiles` (vector tiles)
- `ET_TO.parquet` (GeoParquet) + `ET_TO.pmtiles` (vector tiles)
- `ET_WO.parquet` (GeoParquet) + `ET_WO.pmtiles` (vector tiles)
- `ET_ZZ.parquet` (GeoParquet) + `ET_ZZ.pmtiles` (vector tiles)
