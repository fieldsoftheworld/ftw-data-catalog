# FTW field-boundary predictions — China

Country code: CN. Partitions: 69.

Each polygon carries a `confidence` value (0–100): the modeled PRUE confidence sampled at the field's point-on-surface from the 500 m confidence COG (the `predictions/confidence` collection), rescaled `raw / 0.578178 * 100` and clamped to 100 (cells with no data become null). 0.578178 is treated as 100%, matching the FTW inference app; this centroid sample is the closest single value to the app's `confidence_mean`. Recommended default reliability filter: confidence >= 69 (raw 0.4). Confidence reflects cell-level model reliability, not individual-polygon geometric accuracy. Generated by `add_confidence.py` (+ `process_partition.sh`, `run_rails.sh`, `make_pmtiles.py`): https://github.com/fieldsoftheworld/ftw-portolan/blob/main/scripts/add_confidence.py

## Files
- `CN_01.parquet` (GeoParquet) + `CN_01.pmtiles` (vector tiles)
- `CN_02.parquet` (GeoParquet) + `CN_02.pmtiles` (vector tiles)
- `CN_03.parquet` (GeoParquet) + `CN_03.pmtiles` (vector tiles)
- `CN_04.parquet` (GeoParquet) + `CN_04.pmtiles` (vector tiles)
- `CN_043.parquet` (GeoParquet) + `CN_043.pmtiles` (vector tiles)
- `CN_051.parquet` (GeoParquet) + `CN_051.pmtiles` (vector tiles)
- `CN_053.parquet` (GeoParquet) + `CN_053.pmtiles` (vector tiles)
- `CN_061.parquet` (GeoParquet) + `CN_061.pmtiles` (vector tiles)
- `CN_063.parquet` (GeoParquet) + `CN_063.pmtiles` (vector tiles)
- `CN_065.parquet` (GeoParquet) + `CN_065.pmtiles` (vector tiles)
- `CN_069.parquet` (GeoParquet) + `CN_069.pmtiles` (vector tiles)
- `CN_07.parquet` (GeoParquet) + `CN_07.pmtiles` (vector tiles)
- `CN_09.parquet` (GeoParquet) + `CN_09.pmtiles` (vector tiles)
- `CN_10.parquet` (GeoParquet) + `CN_10.pmtiles` (vector tiles)
- `CN_11.parquet` (GeoParquet) + `CN_11.pmtiles` (vector tiles)
- `CN_13.parquet` (GeoParquet) + `CN_13.pmtiles` (vector tiles)
- `CN_17.parquet` (GeoParquet) + `CN_17.pmtiles` (vector tiles)
- `CN_19.parquet` (GeoParquet) + `CN_19.pmtiles` (vector tiles)
- `CN_33.parquet` (GeoParquet) + `CN_33.pmtiles` (vector tiles)
- `CN_63.parquet` (GeoParquet) + `CN_63.pmtiles` (vector tiles)
- `CN_AH.parquet` (GeoParquet) + `CN_AH.pmtiles` (vector tiles)
- `CN_AL.parquet` (GeoParquet) + `CN_AL.pmtiles` (vector tiles)
- `CN_AMU.parquet` (GeoParquet) + `CN_AMU.pmtiles` (vector tiles)
- `CN_AR.parquet` (GeoParquet) + `CN_AR.pmtiles` (vector tiles)
- `CN_BJ.parquet` (GeoParquet) + `CN_BJ.pmtiles` (vector tiles)
- `CN_CQ.parquet` (GeoParquet) + `CN_CQ.pmtiles` (vector tiles)
- `CN_FJ.parquet` (GeoParquet) + `CN_FJ.pmtiles` (vector tiles)
- `CN_GB.parquet` (GeoParquet) + `CN_GB.pmtiles` (vector tiles)
- `CN_GD.parquet` (GeoParquet) + `CN_GD.pmtiles` (vector tiles)
- `CN_GS.parquet` (GeoParquet) + `CN_GS.pmtiles` (vector tiles)
- `CN_GX.parquet` (GeoParquet) + `CN_GX.pmtiles` (vector tiles)
- `CN_GZ.parquet` (GeoParquet) + `CN_GZ.pmtiles` (vector tiles)
- `CN_HA.parquet` (GeoParquet) + `CN_HA.pmtiles` (vector tiles)
- `CN_HB.parquet` (GeoParquet) + `CN_HB.pmtiles` (vector tiles)
- `CN_HE.parquet` (GeoParquet) + `CN_HE.pmtiles` (vector tiles)
- `CN_HI.parquet` (GeoParquet) + `CN_HI.pmtiles` (vector tiles)
- `CN_HL.parquet` (GeoParquet) + `CN_HL.pmtiles` (vector tiles)
- `CN_HN.parquet` (GeoParquet) + `CN_HN.pmtiles` (vector tiles)
- `CN_JL.parquet` (GeoParquet) + `CN_JL.pmtiles` (vector tiles)
- `CN_JS.parquet` (GeoParquet) + `CN_JS.pmtiles` (vector tiles)
- `CN_JX.parquet` (GeoParquet) + `CN_JX.pmtiles` (vector tiles)
- `CN_LA.parquet` (GeoParquet) + `CN_LA.pmtiles` (vector tiles)
- `CN_LM.parquet` (GeoParquet) + `CN_LM.pmtiles` (vector tiles)
- `CN_LN.parquet` (GeoParquet) + `CN_LN.pmtiles` (vector tiles)
- `CN_NM.parquet` (GeoParquet) + `CN_NM.pmtiles` (vector tiles)
- `CN_NX.parquet` (GeoParquet) + `CN_NX.pmtiles` (vector tiles)
- `CN_O.parquet` (GeoParquet) + `CN_O.pmtiles` (vector tiles)
- `CN_P1.parquet` (GeoParquet) + `CN_P1.pmtiles` (vector tiles)
- `CN_P4.parquet` (GeoParquet) + `CN_P4.pmtiles` (vector tiles)
- `CN_P6.parquet` (GeoParquet) + `CN_P6.pmtiles` (vector tiles)
- `CN_PH.parquet` (GeoParquet) + `CN_PH.pmtiles` (vector tiles)
- `CN_PRI.parquet` (GeoParquet) + `CN_PRI.pmtiles` (vector tiles)
- `CN_QH.parquet` (GeoParquet) + `CN_QH.pmtiles` (vector tiles)
- `CN_SC.parquet` (GeoParquet) + `CN_SC.pmtiles` (vector tiles)
- `CN_SD.parquet` (GeoParquet) + `CN_SD.pmtiles` (vector tiles)
- `CN_SH.parquet` (GeoParquet) + `CN_SH.pmtiles` (vector tiles)
- `CN_SK.parquet` (GeoParquet) + `CN_SK.pmtiles` (vector tiles)
- `CN_SN.parquet` (GeoParquet) + `CN_SN.pmtiles` (vector tiles)
- `CN_SX.parquet` (GeoParquet) + `CN_SX.pmtiles` (vector tiles)
- `CN_TJ.parquet` (GeoParquet) + `CN_TJ.pmtiles` (vector tiles)
- `CN_UK.parquet` (GeoParquet) + `CN_UK.pmtiles` (vector tiles)
- `CN_XJ.parquet` (GeoParquet) + `CN_XJ.pmtiles` (vector tiles)
- `CN_XZ.parquet` (GeoParquet) + `CN_XZ.pmtiles` (vector tiles)
- `CN_Y.parquet` (GeoParquet) + `CN_Y.pmtiles` (vector tiles)
- `CN_YEV.parquet` (GeoParquet) + `CN_YEV.pmtiles` (vector tiles)
- `CN_YN.parquet` (GeoParquet) + `CN_YN.pmtiles` (vector tiles)
- `CN_ZAB.parquet` (GeoParquet) + `CN_ZAB.pmtiles` (vector tiles)
- `CN_ZJ.parquet` (GeoParquet) + `CN_ZJ.pmtiles` (vector tiles)
- `CN_ZZ.parquet` (GeoParquet) + `CN_ZZ.pmtiles` (vector tiles)
