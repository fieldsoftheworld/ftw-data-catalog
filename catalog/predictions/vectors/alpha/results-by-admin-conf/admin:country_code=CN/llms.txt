# FTW field-boundary predictions — China

Part of [Fields of the World](https://fieldsofthe.world) — agricultural field boundaries delineated by the PRUE model from Sentinel-2 imagery.

Country code: CN. Partitions: 69.

A GeoParquet vector dataset is derived from the [prediction Zarr](https://data.source.coop/ftw/global-data/predictions/zarr/collection.json) by thresholding the softmax outputs for [non_field_background, field, field_boundaries] at 0.5 and polygonizing.

## Columns
Definitions use the fiboa core spec and vecorel extensions ([fiboa core](https://github.com/fiboa/specification/blob/main/core/README.md), [geometry-metrics](https://github.com/vecorel/geometry-metrics-extension), [administrative-division](https://github.com/vecorel/administrative-division-extension)):
- `id` (string): An identifier for the field. Must be unique per collection. ([fiboa core](https://github.com/fiboa/specification/blob/main/core/README.md))
- `geometry` (binary): A geometry that reflects the footprint of the field, usually a Polygon. Default CRS is WGS84. ([fiboa core](https://github.com/fiboa/specification/blob/main/core/README.md))
- `bbox` (struct): The bounding box of the field. ([fiboa core](https://github.com/fiboa/specification/blob/main/core/README.md))
- `metrics:area` (float): Area of the field, in square meters (m²). Must be > 0. ([vecorel geometry-metrics](https://github.com/vecorel/geometry-metrics-extension))
- `metrics:perimeter` (float): Perimeter of the field, in meters (m). Must be > 0. ([vecorel geometry-metrics](https://github.com/vecorel/geometry-metrics-extension))
- `determination:datetime` (timestamp): The last timestamp at which the field did exist and was observed, in the UTC timezone. ([fiboa core](https://github.com/fiboa/specification/blob/main/core/README.md))
- `determination:method` (string): The boundary creation method (one of: manual, surveyed, driven, auto-operation, auto-imagery, unknown). ([fiboa core](https://github.com/fiboa/specification/blob/main/core/README.md))
- `admin:country_code` (string): ISO 3166-1 alpha-2 country code (aka admin0). Two-letter country code for the country that contains the field. ([vecorel administrative-division](https://github.com/vecorel/administrative-division-extension))
- `admin:subdivision_code` (string): ISO 3166-2 code for the principal subdivision (e.g. province or state, aka admin1) of a country that contains the field. Only the subdivision part of the code is stored. ([vecorel administrative-division](https://github.com/vecorel/administrative-division-extension))
- `confidence` (float): Derived; not part of the upstream model output. Modeled PRUE confidence on a 0–100 scale, sampled at the field's point-on-surface from the 500 m confidence COG ([predictions/confidence](https://data.source.coop/ftw/global-data/predictions/confidence/collection.json)) and rescaled `raw / 0.578178 * 100`, clamped to 100 (0.578178 is treated as 100%, matching the FTW inference app; this centroid sample is the closest single value to the app's `confidence_mean`); **null where the field falls outside the modeled-confidence layer's coverage — i.e. no value there, not a low score** (the layer is sparse and conservative, fully covering mainly the 24 FTW-labelled training countries). Recommended reliability filter: `confidence >= 69` (raw 0.4). Reflects 500 m cell-level model reliability, not individual-polygon geometric accuracy. Generated by [add_confidence.py](https://github.com/fieldsoftheworld/ftw-data-catalog/blob/main/scripts/confidence/add_confidence.py) (+ process_partition.sh, run_rails.sh, make_pmtiles.py).

## Files
- `CN_01.parquet` (GeoParquet) + `CN_01.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_01.*.style.json` (green/confidence × 2024/2025)
- `CN_02.parquet` (GeoParquet) + `CN_02.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_02.*.style.json` (green/confidence × 2024/2025)
- `CN_03.parquet` (GeoParquet) + `CN_03.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_03.*.style.json` (green/confidence × 2024/2025)
- `CN_04.parquet` (GeoParquet) + `CN_04.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_04.*.style.json` (green/confidence × 2024/2025)
- `CN_043.parquet` (GeoParquet) + `CN_043.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_043.*.style.json` (green/confidence × 2024/2025)
- `CN_051.parquet` (GeoParquet) + `CN_051.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_051.*.style.json` (green/confidence × 2024/2025)
- `CN_053.parquet` (GeoParquet) + `CN_053.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_053.*.style.json` (green/confidence × 2024/2025)
- `CN_061.parquet` (GeoParquet) + `CN_061.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_061.*.style.json` (green/confidence × 2024/2025)
- `CN_063.parquet` (GeoParquet) + `CN_063.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_063.*.style.json` (green/confidence × 2024/2025)
- `CN_065.parquet` (GeoParquet) + `CN_065.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_065.*.style.json` (green/confidence × 2024/2025)
- `CN_069.parquet` (GeoParquet) + `CN_069.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_069.*.style.json` (green/confidence × 2024/2025)
- `CN_07.parquet` (GeoParquet) + `CN_07.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_07.*.style.json` (green/confidence × 2024/2025)
- `CN_09.parquet` (GeoParquet) + `CN_09.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_09.*.style.json` (green/confidence × 2024/2025)
- `CN_10.parquet` (GeoParquet) + `CN_10.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_10.*.style.json` (green/confidence × 2024/2025)
- `CN_11.parquet` (GeoParquet) + `CN_11.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_11.*.style.json` (green/confidence × 2024/2025)
- `CN_13.parquet` (GeoParquet) + `CN_13.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_13.*.style.json` (green/confidence × 2024/2025)
- `CN_17.parquet` (GeoParquet) + `CN_17.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_17.*.style.json` (green/confidence × 2024/2025)
- `CN_19.parquet` (GeoParquet) + `CN_19.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_19.*.style.json` (green/confidence × 2024/2025)
- `CN_33.parquet` (GeoParquet) + `CN_33.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_33.*.style.json` (green/confidence × 2024/2025)
- `CN_63.parquet` (GeoParquet) + `CN_63.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_63.*.style.json` (green/confidence × 2024/2025)
- `CN_AH.parquet` (GeoParquet) + `CN_AH.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_AH.*.style.json` (green/confidence × 2024/2025)
- `CN_AL.parquet` (GeoParquet) + `CN_AL.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_AL.*.style.json` (green/confidence × 2024/2025)
- `CN_AMU.parquet` (GeoParquet) + `CN_AMU.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_AMU.*.style.json` (green/confidence × 2024/2025)
- `CN_AR.parquet` (GeoParquet) + `CN_AR.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_AR.*.style.json` (green/confidence × 2024/2025)
- `CN_BJ.parquet` (GeoParquet) + `CN_BJ.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_BJ.*.style.json` (green/confidence × 2024/2025)
- `CN_CQ.parquet` (GeoParquet) + `CN_CQ.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_CQ.*.style.json` (green/confidence × 2024/2025)
- `CN_FJ.parquet` (GeoParquet) + `CN_FJ.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_FJ.*.style.json` (green/confidence × 2024/2025)
- `CN_GB.parquet` (GeoParquet) + `CN_GB.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_GB.*.style.json` (green/confidence × 2024/2025)
- `CN_GD.parquet` (GeoParquet) + `CN_GD.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_GD.*.style.json` (green/confidence × 2024/2025)
- `CN_GS.parquet` (GeoParquet) + `CN_GS.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_GS.*.style.json` (green/confidence × 2024/2025)
- `CN_GX.parquet` (GeoParquet) + `CN_GX.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_GX.*.style.json` (green/confidence × 2024/2025)
- `CN_GZ.parquet` (GeoParquet) + `CN_GZ.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_GZ.*.style.json` (green/confidence × 2024/2025)
- `CN_HA.parquet` (GeoParquet) + `CN_HA.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_HA.*.style.json` (green/confidence × 2024/2025)
- `CN_HB.parquet` (GeoParquet) + `CN_HB.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_HB.*.style.json` (green/confidence × 2024/2025)
- `CN_HE.parquet` (GeoParquet) + `CN_HE.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_HE.*.style.json` (green/confidence × 2024/2025)
- `CN_HI.parquet` (GeoParquet) + `CN_HI.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_HI.*.style.json` (green/confidence × 2024/2025)
- `CN_HL.parquet` (GeoParquet) + `CN_HL.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_HL.*.style.json` (green/confidence × 2024/2025)
- `CN_HN.parquet` (GeoParquet) + `CN_HN.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_HN.*.style.json` (green/confidence × 2024/2025)
- `CN_JL.parquet` (GeoParquet) + `CN_JL.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_JL.*.style.json` (green/confidence × 2024/2025)
- `CN_JS.parquet` (GeoParquet) + `CN_JS.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_JS.*.style.json` (green/confidence × 2024/2025)
- `CN_JX.parquet` (GeoParquet) + `CN_JX.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_JX.*.style.json` (green/confidence × 2024/2025)
- `CN_LA.parquet` (GeoParquet) + `CN_LA.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_LA.*.style.json` (green/confidence × 2024/2025)
- `CN_LM.parquet` (GeoParquet) + `CN_LM.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_LM.*.style.json` (green/confidence × 2024/2025)
- `CN_LN.parquet` (GeoParquet) + `CN_LN.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_LN.*.style.json` (green/confidence × 2024/2025)
- `CN_NM.parquet` (GeoParquet) + `CN_NM.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_NM.*.style.json` (green/confidence × 2024/2025)
- `CN_NX.parquet` (GeoParquet) + `CN_NX.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_NX.*.style.json` (green/confidence × 2024/2025)
- `CN_O.parquet` (GeoParquet) + `CN_O.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_O.*.style.json` (green/confidence × 2024/2025)
- `CN_P1.parquet` (GeoParquet) + `CN_P1.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_P1.*.style.json` (green/confidence × 2024/2025)
- `CN_P4.parquet` (GeoParquet) + `CN_P4.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_P4.*.style.json` (green/confidence × 2024/2025)
- `CN_P6.parquet` (GeoParquet) + `CN_P6.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_P6.*.style.json` (green/confidence × 2024/2025)
- `CN_PH.parquet` (GeoParquet) + `CN_PH.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_PH.*.style.json` (green/confidence × 2024/2025)
- `CN_PRI.parquet` (GeoParquet) + `CN_PRI.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_PRI.*.style.json` (green/confidence × 2024/2025)
- `CN_QH.parquet` (GeoParquet) + `CN_QH.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_QH.*.style.json` (green/confidence × 2024/2025)
- `CN_SC.parquet` (GeoParquet) + `CN_SC.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_SC.*.style.json` (green/confidence × 2024/2025)
- `CN_SD.parquet` (GeoParquet) + `CN_SD.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_SD.*.style.json` (green/confidence × 2024/2025)
- `CN_SH.parquet` (GeoParquet) + `CN_SH.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_SH.*.style.json` (green/confidence × 2024/2025)
- `CN_SK.parquet` (GeoParquet) + `CN_SK.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_SK.*.style.json` (green/confidence × 2024/2025)
- `CN_SN.parquet` (GeoParquet) + `CN_SN.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_SN.*.style.json` (green/confidence × 2024/2025)
- `CN_SX.parquet` (GeoParquet) + `CN_SX.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_SX.*.style.json` (green/confidence × 2024/2025)
- `CN_TJ.parquet` (GeoParquet) + `CN_TJ.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_TJ.*.style.json` (green/confidence × 2024/2025)
- `CN_UK.parquet` (GeoParquet) + `CN_UK.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_UK.*.style.json` (green/confidence × 2024/2025)
- `CN_XJ.parquet` (GeoParquet) + `CN_XJ.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_XJ.*.style.json` (green/confidence × 2024/2025)
- `CN_XZ.parquet` (GeoParquet) + `CN_XZ.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_XZ.*.style.json` (green/confidence × 2024/2025)
- `CN_Y.parquet` (GeoParquet) + `CN_Y.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_Y.*.style.json` (green/confidence × 2024/2025)
- `CN_YEV.parquet` (GeoParquet) + `CN_YEV.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_YEV.*.style.json` (green/confidence × 2024/2025)
- `CN_YN.parquet` (GeoParquet) + `CN_YN.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_YN.*.style.json` (green/confidence × 2024/2025)
- `CN_ZAB.parquet` (GeoParquet) + `CN_ZAB.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_ZAB.*.style.json` (green/confidence × 2024/2025)
- `CN_ZJ.parquet` (GeoParquet) + `CN_ZJ.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_ZJ.*.style.json` (green/confidence × 2024/2025)
- `CN_ZZ.parquet` (GeoParquet) + `CN_ZZ.pmtiles` (vector tiles, 2024/2025 layers) + 4 `CN_ZZ.*.style.json` (green/confidence × 2024/2025)
